@model IEnumerable<SSD_Forum.Models.Reply>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserWhoReplied)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateReplied)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Content)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserWhoReplied)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateReplied)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReplyId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReplyId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReplyId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
